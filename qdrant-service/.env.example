# synaplan-memories / qdrant-service
# Minimal environment configuration.
#
# Usage:
#   - Development: copy to .env (or use docker-compose env overrides)
#   - Production:  set real values via your secrets manager

# Qdrant connection (inside docker-compose this is usually correct)
QDRANT_URL=http://qdrant:6334
# QDRANT_API_KEY=   # optional (only if your Qdrant has auth enabled)

# Service HTTP port (container internal)
PORT=8090

# Authentication for synaplan backend -> qdrant-service requests (X-API-Key)
# IMPORTANT: set a strong key in production (e.g. `openssl rand -hex 32`)
SERVICE_API_KEY=changeme-in-production

# Optional: webhook alerts + daily stats (Discord/Slack/...)
# WEBHOOK_URL=
# ENABLE_DAILY_STATS=false
# STATS_INTERVAL_HOURS=24

# Optional: logging verbosity
# RUST_LOG=synaplan_qdrant_service=info,tower_http=info
